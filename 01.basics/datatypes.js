"use strict"; // treat all JS code as newer version

// alert( 3 + 3) // we are using nodejs, not browser

console.log(3 //dont write code in this from because the code is not just about writing,it also emphasis on code readiability and efficiency
    +
     3) // code readability should be high

console.log("Hitesh")
let a=10
let b="22"
let c=10
console.log(a+c+b)
console.log()

let name = "hitesh"//" " ->this is a string
let age = 18// 18 is a number
let isLoggedIn = false
let state;
/* =>primitive data types
// number => 2 to power 53
// bigint 
// string => "" also strings reads the whole word in quotes( "ws1234@@")->so this is a complete one string
// example of string is **PASSWORD**
// boolean => true/false
// null => standalone value//ye kud mein ek value hai
// undefined => // u have not defined anything thats the undefined called
// symbol => unique

*/
// object

console.log(typeof undefined); // undefined
console.log(typeof null); // object
//null ka type object hai
